# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# build output
dist/
build/

# database — SQLite is the system of record but the file itself is local
data/*.sqlite
data/**/*.sqlite
# baseline snapshots taken before a measured crawl (scripts/fuzz46-per-code.ts)
data/fuzz46/
data/*.sqlite-journal
data/*.sqlite-wal
data/*.sqlite-shm
data/*.db
# the pilot database as it is attached to an issue, and any other dump
data/*.sqlite.gz
data/*.gz

# Review-UI screenshots. They render real company names and real phone
# numbers, so they are lead data like any other and are delivered as issue
# attachments, never committed. Regenerate with scripts/fuzz25-screenshots.ts.
screenshots/

# generated artifacts
exports/
*.xlsx

# environment
.env
.env.*
!.env.example

# tooling caches
coverage/
.vitest/
.eslintcache
*.tsbuildinfo

# playwright
test-results/
playwright-report/
.playwright/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# scraper caches — the Overture extract is re-derivable, and large
data/cache/

# Pilot run artefacts — fetched pages and reports containing lead data.
runlogs/
