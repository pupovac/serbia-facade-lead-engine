# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# build output
dist/
build/

# database — SQLite is the system of record but the file itself is local
data/*.sqlite
data/*.sqlite-journal
data/*.sqlite-wal
data/*.sqlite-shm
data/*.db

# generated artifacts
exports/
*.xlsx

# environment
.env
.env.*
!.env.example

# tooling caches
coverage/
.vitest/
.eslintcache
*.tsbuildinfo

# playwright
test-results/
playwright-report/
.playwright/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# scraper caches — the Overture extract is re-derivable, and large
data/cache/

# Pilot run artefacts — fetched pages and reports containing lead data.
runlogs/

# Scratch: replay databases and downloaded corpora. Lead data, never committed.
tmp/
data/*.gz
